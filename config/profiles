[default]
# each line is executed by initialize individually based on the config(s) selected
PREREQ="$BASH_FILES_DIR/config/os-specific/auto-detect/prerequisites"
chmod +x $PREREQ && $PREREQ
echo "SUCCESS: Initialized default profile"

[macos]
echo "Mac OS specific files"

[mac_dp]
export BFCONFIG_FILE_OVERWRITE="$BASH_FILES_DIR/config/machine-specific/mac_dp/.bfconfig"

[vm1]
echo "test"
echo "test2"
